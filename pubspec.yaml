name: sticker_chat
description: Animated stickers powered by Rive and Stream

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4
  stream_chat_flutter: ^3.3.2
  rive: ^0.8.1

dependency_overrides:
  visibility_detector: ^0.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
     - assets/
  #   - images/a_dot_ham.jpeg